function validateEqual(item1, item2) {
    if (item1 === item2) {
        return true;
    }
}

function verifyStateChanged(elements, originalValues, elementToEnable) {
    // Get elements by jquery id

    // Get current state of objects

    // Get original state of objects from list

    // If new state is not equal to old state then enable
}